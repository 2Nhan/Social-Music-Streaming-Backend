type: object
required:
  - targetType
  - targetId
properties:
  targetType:
    $ref: '../response/TargetType.yml'
  targetId:
    type: string
    description: ID of the target entity (song or playlist).
